# -------------------------------
# Backend (NestJS / apps/api)
# -------------------------------

# API server port
API_PORT=3000

# Allowed origins for CORS (comma-separated)
# Example: local dev, vercel preview, production
CORS_ORIGIN=http://localhost:4200,https://myfamilytable.vercel.app,https://myfamilytable.app

# Database connection string (replace USER, PASSWORD, HOST, DB with your actual values in local/prod .env files)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require